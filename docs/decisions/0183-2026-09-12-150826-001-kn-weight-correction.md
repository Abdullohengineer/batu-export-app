## 2026-09-12 — 150826-001 (Isfara P3) KN weight correction

**Context:** Serial `150826-001` (sent 7,947 kg, wash cycle closed 2026-08-29 10:10:35) had
its single KN pallet booked at 1,420 kg; the real weight per the paper log is 1,250 kg — the
170 kg overage was a data-entry error, never physically existed. Pure within-serial
correction, no cross-serial movement. Full SQL archived at
`docs/data-corrections/2026-09-12_150826-001_kn-weight-correction.sql`.

### Mechanism

Voided `PLT-150826-001-KN-1` (1,420) and inserted `PLT-150826-001-KN-2` (1,250, same
`received_date` as `KN-1` — no date drift). K1/K4/K6/K8 untouched. 2 audit_log rows (1 void,
1 insert), `reason: "Correct KN weight -- 170 kg overage was data-entry error, never
physical"`.

**Confirmed before writing:** cycle is closed (`closed_at` set, unchanged by this
correction — same closed-cycle handling established in 0182: `finished_pallets` has no
schema dependency on `wash_cycles.closed_at`, and `final_loss_pct`/`finalized_at` are dead
columns with nothing to recompute). `KN-1` carries zero downstream references
(`chiqim_pallet_consumption`/`serial_mint_sources`/`lab_results.sampled_pallet`). The K4
pallet (`PLT-150826-001-04-1`, 4,260 kg, passport-derived status `band_qilingan`/reserved —
partially dispatched, 490 kg still available / 3,770 kg dispatched per
`get_serial_passport`) is a separate, unrelated row; confirmed unaffected by this change.

### Verification

Dry-run (`BEGIN...ROLLBACK`) shown to the user before applying for real. Post-apply:

| | Sent | K1 | K4 | K6 | K8 | KN | Output | Loss | Loss % |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 150826-001 | 7,947 | 390 | 4,260 | 1,220 | 90 | 1,250 | 7,210 | 737 | 9.27% |

Mass balance holds (7,947 = 7,210 + 737). Also verified: `kirim_line_calibre_output` returns
the corrected breakdown; `get_serial_passport`'s `returnedKg`=7,210, `byCalibre` matches,
`lossKg`=737, `isRealized: true`, K4's `band_qilingan` status and 490/3,770
available/dispatched split unchanged; `report_moyka_output_rows_by_serial` (Hisobot
MOYKADAN) returns 7,210; `yield_rows` returns `output_kg`=7,210, `loss_kg`=737,
`loss_pct`=9.3 (view rounds to one decimal, matches 9.27% within that precision); raw
passport pallet list shows `KN-1` labeled `bekor_qilingan` and `KN-2` `omborda` — void filter
still excludes it from display.
