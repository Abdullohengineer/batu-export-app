## 2026-09-09 — 040926-002 K1/KN split correction, revises 0179

**Context:** `docs/decisions/0179-2026-09-08-030926-001-040926-002-pallet-weight-corrections.md`
already corrected `040926-002`'s total output/loss (K1 4,820/KN 780 → K1 4,530/KN 1,690,
output 6,220, loss 330 kg/5.04%). A closer read of the paper log showed the *split* between
K1 and KN within that same total was still wrong: real numbers are K1=4,930/KN=1,290 — the
total output (6,220) and loss (330 kg/5.04%) are unchanged, only a 400 kg shift between the
two kalibres. Full SQL archived at
`docs/data-corrections/2026-09-09_040926-002_k1-kn-split-correction.sql`.

**Stale task input caught before writing:** the task requesting this correction named the
pre-0179 barcodes (`PLT-040926-002-01-1`, `-01-2`, `-KN-1`, `-KN-2`) as the pallets to void —
but those were already voided by 0179 and are not live. The actual active pallets are
`PLT-040926-002-01-3` (4,530, K1) and `PLT-040926-002-KN-3` (1,690, KN), the replacements 0179
itself inserted. Flagged to the user before writing; confirmed to void the current active
pallets instead of the stale ones named in the request.

### Mechanism

Voided `PLT-040926-002-01-3` (4,530) and `PLT-040926-002-KN-3` (1,690), inserted
`PLT-040926-002-01-4` (4,930, K1) and `PLT-040926-002-KN-4` (1,290, KN), both dated
2026-09-08 (the `received_date` shared by both voided pallets — no date drift). 4 audit_log
rows (2 void, 2 insert).

**Confirmed before writing:** wash cycle is closed (`closed_at = 2026-09-08 14:06:44`);
`final_loss_pct`/`finalized_at` are both null, so there was no cached loss value to update —
loss is derived live. `yield_rows` (migration 0030) is a view over `finished_pallets`, not a
stored/cached table, so it recomputes automatically once the pallets change; no separate
recompute step was needed. Zero downstream references
(`chiqim_pallet_consumption`/`serial_mint_sources`/`lab_results.sampled_pallet`) on either
voided pallet.

### Verification

Dry-run (`BEGIN...ROLLBACK`) shown to the user before applying for real. Post-apply:
`kirim_line_calibre_output('040926-002')` returns K1:4,930/KN:1,290;
`get_serial_passport`'s `returnedKg`=6,220, `byCalibre` matches, cycle `lossKg`=330;
`report_moyka_output_rows_by_serial` (Hisobot MOYKADAN) returns 6,220; `yield_rows` returns
`output_kg`=6,220, `loss_kg`=330, `loss_pct`=5.0, `live_calibre_kg`=4,930,
`live_konditirskiy_kg`=1,290, `calibre_mix` matching (79.3%/20.7%). Mass balance: 6,220 + 330
= 6,550 sent, unchanged from 0179.
