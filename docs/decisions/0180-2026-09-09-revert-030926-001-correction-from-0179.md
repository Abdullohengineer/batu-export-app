## 2026-09-09 — Revert 030926-001's leg of 0179 (incomplete information)

**Context:** `docs/decisions/0179-2026-09-08-030926-001-040926-002-pallet-weight-corrections.md`
treated `030926-001`'s booked pallets as a data-entry error and replaced 6 of them. This was
based on incomplete information: the current booked pallets were actually all correct — they
represent two production sessions combined (an earlier session the user had forgotten about,
plus a recent one), not a mistake. Full SQL archived at
`docs/data-corrections/2026-09-09_030926-001_revert-0179-correction.sql`.

**Decision:** revert 030926-001's leg of 0179 in full. `040926-002`'s correction (the other
half of 0179) is untouched — that one was a genuine data-entry error and stands.

### Mechanism

Un-voided the 6 pallets 0179 had voided (`PLT-030926-001-01-1` 170, `-02-1` 1,370, `-04-1`
350, `-KN-1` 460, `-KN-2` 370, `-KN-3` 180) back to `in_stock`/`voided_at=NULL`, and voided the
one pallet 0179 had inserted (`PLT-030926-001-KN-4`, 550). Confirmed before writing that
`KN-4` carries zero downstream references (`chiqim_pallet_consumption`,
`serial_mint_sources`, `lab_results.sampled_pallet`), so voiding it needed no unwinding.
7 audit_log rows (6 un-void, 1 void), `reason: "Revert of previous correction — original
bookings were correct"`.

Dry-run (`BEGIN...ROLLBACK`) shown to the user before applying for real.

### Verification

Post-apply state matches the pre-0179 original exactly: K1=170, K2=2,090 (1,370+720), K4=1,960
(350+1,610), KN=1,010 (460+370+180) = 5,230 output. Wash cycle (`id 0e2093af-...`) confirmed
still `status='active'`, `closed_at`/`finalized_at` null throughout — untouched by either 0179
or this revert. Mass balance: 5,230 output + 3,062 still-in-moyka = 8,292 sent, no loss booked
(cycle still open).

Also verified: `kirim_line_calibre_output('030926-001')` returns
K1:170/K2:2,090/K4:1,960/KN:1,010; `get_serial_passport`'s `returnedKg`=5,230, `byCalibre`
matches, `voidedKg`=550 (only the reverted-out `KN-4`), `isRealized: false`, `closedAt: null`;
`report_moyka_output_rows_by_serial` (Hisobot MOYKADAN) returns 5,230; `moyka_sends` unchanged
at 8,292 throughout.
