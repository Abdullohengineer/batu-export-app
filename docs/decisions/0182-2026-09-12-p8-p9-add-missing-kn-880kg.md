## 2026-09-12 — P8/P9 add missing KN output (880 kg), equal-loss split

**Context:** 880 kg of KN output was omitted from the 0178 P8/P9 supersede. Adding it now,
split to equalize loss % across both serials — no change to K1/K2/K4/K6/K8. Both wash cycles
are closed (`closed_at` set 2026-09-09 ~05:53 UTC — confirmed via a read-only diagnostic
session run just before this task, which also confirmed the closures were legitimate manual
Yakunlash actions, not a reconciliation regression). Full SQL archived at
`docs/data-corrections/2026-09-12_p8-p9-add-missing-kn-880kg.sql`.

### Mechanism — purely additive, no existing pallet touched

Inserted `PLT-110826-003-KN-5` (750 kg, dated 2026-09-03 — P8's finish date, matching
`KN-4`) and `PLT-180826-001-KN-5` (130 kg, dated 2026-09-05 — P9's finish/mixing date,
matching `KN-4`). Neither existing KN pallet (`PLT-110826-003-KN-4` 1,260,
`PLT-180826-001-KN-4` 1,270) was voided or modified. 2 audit_log rows (both
`insert_correction`), `reason: "Add missing KN output to reconciled batch -- 880 kg omitted
from previous supersede, equal-loss split"`.

**Closed-cycle handling, confirmed before writing:** `finished_pallets` has zero triggers and
no foreign key to `wash_cycles` — inserting a new active pallet has no dependency on
`closed_at` and needed no special handling. `wash_cycles.final_loss_pct`/`finalized_at` are
confirmed dead columns (a full-text search of every function body found zero references to
`final_loss_pct`; migration 0101 removed the pre-existing locked-loss "Tugallash" design in
favor of computing loss live everywhere), so there was nothing to recompute or cache — every
consumer (`yield_rows`, `client_serial_loss_kg`, `get_serial_passport`,
`kirim_line_calibre_output`, Hisobot MOYKADAN) derives output/loss live from
`finished_pallets` at query time and picked up the new totals automatically. Confirmed
post-apply that `wash_cycles` itself is byte-identical to before (same `closed_at`
timestamps, `status`/`final_loss_pct`/`finalized_at` all unchanged) — the cycles were never
reopened.

### Verification

Dry-run (`BEGIN...ROLLBACK`) shown to the user before applying for real. Post-apply:

| | Sent | K1 | K2 | K4 | K6 | K8 | KN | Output | Loss | Loss % |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 110826-003 (P8) | 7,190 | 800 | 760 | 3,020 | 0 | 60 | 2,010 | 6,650 | 540 | 7.51% |
| 180826-001 (P9) | 7,960 | 0 | 320 | 5,020 | 580 | 40 | 1,400 | 7,360 | 600 | 7.54% |
| Combined | 15,150 | | | | | | 3,410 | 14,010 | 1,140 | 7.53% |

Mass balance holds exactly (7,190 = 6,650 + 540; 7,960 = 7,360 + 600). Also verified:
`kirim_line_calibre_output` returns the corrected KN totals for both serials;
`get_serial_passport`'s `returnedKg`/`byCalibre` match, `lossKg`=540/600, `isRealized: true`
for both (cycle still closed, gap now reads as booked/realized loss);
`report_moyka_output_rows_by_serial` (Hisobot MOYKADAN) returns 6,650/7,360; `yield_rows`
returns `output_kg`=6,650/7,360, `loss_kg`=540/600, `loss_pct`=7.5/7.5 (view rounds to one
decimal, matches 7.51%/7.54% within that precision).
