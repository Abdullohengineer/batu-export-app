## 2026-09-08 — Pallet weight corrections: 030926-001 (open cycle) and 040926-002 (closed cycle)

**Context:** Two unrelated within-serial data-entry corrections, shipped together in one commit.
Full SQL archived at
`docs/data-corrections/2026-09-08_030926-001_040926-002_pallet-weight-corrections.sql`.

### 040926-002 (natural, sent 6,550 kg, cycle closed/realized)

Booked K1=4,820 / KN=780 (loss 950 kg / 14.50%) was a pure data-entry error. Paper log says
K1=4,530 / KN=1,690 (loss 330 kg / 5.04%). No cross-serial movement, no other kalibres
involved.

Voided the 4 current pallets (`PLT-040926-002-01-1` 510, `-01-2` 4,310, `-KN-1` 110, `-KN-2`
670) and inserted 2 replacements at the corrected weights (`PLT-040926-002-01-3` 4,530,
`PLT-040926-002-KN-3` 1,690), both dated 2026-09-08 — the `received_date` shared by all 4
original pallets, so no date drift. 6 audit_log rows (4 void, 2 insert).

Mass balance: 6,220 output + 330 loss = 6,550 sent. Matches the wash cycle's already-closed
`lossKg: 330` in `get_serial_passport`.

### 030926-001 (natural, sent 8,292 kg, wash cycle OPEN — `closed_at` null, `status='active'`)

Booked output across K1/K2/K4/KN was wrong; real output so far per paper log is K2=720,
K4=1,610, KN=550, no K1, with 5,412 kg still in moyka (no loss booked — cycle isn't closed,
loss finalizes at cycle close, not by this correction).

Voided 6 pallets (`PLT-030926-001-01-1` 170, `-02-1` 1,370, `-04-1` 350, `-KN-1` 460, `-KN-2`
370, `-KN-3` 180), kept `-02-2` (K2 720) and `-04-2` (K4 1,610) unchanged since they already
matched the paper log, and inserted one corrected KN pallet (`PLT-030926-001-KN-4` 550, dated
2026-09-04 — the earliest `received_date` among the 3 voided KN pallets, for continuity). 7
audit_log rows (6 void, 1 insert).

**Confirmed before writing:** wash cycle `030926-001` (`id 0e2093af-...`) is `status='active'`,
`closed_at`/`finalized_at` both null — confirmed still true after the correction, i.e. this
change did not touch the cycle's open/closed state. Zero `chiqim_pallet_consumption`,
`serial_mint_sources`, or pallet-keyed `lab_results` rows reference any of the 6 voided
pallets on this serial (nor the 4 voided on 040926-002) — nothing needed unwinding first.

Mass balance: 2,880 output + 5,412 still-in-moyka = 8,292 sent, no loss booked.

### Verification

Both serials dry-run (`BEGIN...ROLLBACK`) and shown to the user before applying for real; both
applied in one `DO $$ ... $$` block (13 total writes, each with its own paired `audit_log`
row — confirmed 13 rows written). Post-apply, for both serials:
`kirim_line_calibre_output` and `get_serial_passport`'s `returnedKg`/`byCalibre` match target
exactly; `report_moyka_output_rows_by_serial` (Hisobot MOYKADAN) returns 6,220 / 2,880;
`030926-001`'s wash cycle still reads `isRealized: false`, `closedAt: null` in the passport
(untouched); `SerialPassportModal.tsx`'s `bekor_qilingan` filter (from 0167) still excludes
every voided pallet from the passport pallet list.
